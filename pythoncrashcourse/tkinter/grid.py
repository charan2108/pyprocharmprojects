from tkinter import *

root = Tk()
# creating label widget
label = Label(root, text="Hello world").grid(row=0, column=0)
label1 = Label(root, text="my name is code camp").grid(row=0, column=0)
label2 = Label(root, text="").grid(row=0, column=0)
# Shoving on to thw screen
# label.grid(row=0, column=0)
# label1.grid(row=1, column=1)
# label2.grid(row=1, column=3)
root.mainloop()

